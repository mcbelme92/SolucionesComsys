/**
 * Interfaz que representa las props para el componente Home.
 * @interface HomeProps
 * @property {string} title - El título que se mostrará en el componente Home.
 */

export interface HomeProps {
  title: string;
}
